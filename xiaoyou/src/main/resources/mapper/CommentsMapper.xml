<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 使用映射接口的方式， 命名空间的值必须与映射接口的全限定名一致 -->
<mapper namespace="xiaoyou.mapper.CommentsMapper">
	<!-- 添加评论 -->
	<insert id="getInsertComments" parameterType="CommentsPO">
		 insert into comments values(seq_c_id.nextval,#{talk_id},#{c_text},#{talking_id},sysdate)
	</insert>

	
	
	
</mapper>